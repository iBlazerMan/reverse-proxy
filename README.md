# Reverse Proxy

  A lightweight reverse proxy server to route and balance HTTP traffic across multiple backend services. Supports round robin, least connection, and other custom load balancing schemes.

  To run, navigate to `cmd/proxy` and run `go run main.go`.

